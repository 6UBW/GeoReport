<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ubw6.com.georeport" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".KevinMainActivity"
            android:label="@string/main_app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".KevinLoginActivity"
            android:label="@string/login_app_name" >
        </activity>
        <activity
            android:name=".KevinAccountActivity"
            android:label="@string/myacc_app_name" >
        </activity>
        <activity
            android:name=".KevinRegisterActivity"
            android:label="@string/register_app_name" >
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
